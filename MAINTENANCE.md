# 株価あそび場 保守マニュアル

この文書は、https://github.com/abcderp2/stocktrading0 を無料プランのAIとエントリークラスのスマートフォンまたはタブレットでも保守しやすくするための正本です。別の保守手順書を増やさず、手順変更はこのファイルへ統合します。

公開サイト

https://abcderp2.github.io/stocktrading0/

## 1. 最優先方針

- `main`は公開中の安定版として扱う
- 通常変更は作業ブランチからPull Requestで行う
- 追加課金、有料API、広告、アクセス解析、外部CDN、外部フォント、外部JavaScriptを追加しない
- npm、フレームワーク、ビルドツールを必須にしない
- エントリークラスのスマートフォン、タブレットを性能基準から外さない
- 画面を見た利用者が説明書なしでも主要操作を発見できる状態を維持する
- 複数企業管理を再導入しない。現在の中心は1つの「テスト企業」
- 金融数値が非現実的でも仕様として許可する。安全上限と金融上の現実性を混同しない

## 2. AIへ渡す依頼文

```text
株価あそび場を保守してください。
正本は https://github.com/abcderp2/stocktrading0 です。
最初に MAINTENANCE.md を読み、必要なら SECURITY.md も読んでください。
現在の1企業モード、直感的なスライダー操作、チャート操作、低性能端末向けの軽量構成を維持してください。
mainへ直接書き込まず作業ブランチを作り、必要な変更、検査、Pull Request、検査成功確認、マージ、公開確認まで進めてください。
今回の依頼内容:
ここへ依頼を書く
```

## 3. 参照順位

1. 実際の動作と`index.html`、`assets/js/`、`assets/css/`
2. セキュリティは`SECURITY.md`
3. 保守方法は`MAINTENANCE.md`
4. 概要は`README.md`
5. 更新履歴は`CHANGELOG.md`

資料が食い違う場合は、公開中の安全な動作を優先し、推測で仕様を増やしません。

## 4. 現在のUI原則

画面は次の順番で理解できることを基準にします。

1. サイト名「株価あそび場」
2. 3つだけの使い方
3. 「テスト企業」の名前変更とチャート
4. 株価などのスライダー
5. 時間進行
6. 買う、売る
7. 詳細設定
8. 保存とバックアップ

主要設定はスライダーを動かした瞬間に反映します。「編集してから反映」のような中間操作を増やしません。

会社の追加、削除、選択画面はありません。初期名は「テスト企業」で、利用者が自由に上書きします。

## 5. JavaScriptの役割

- `market-engine.js`: 1企業の市場生成、現在株価変更、時間進行、売買、ポートフォリオ
- `chart.js`: Canvasローソク足、横移動、拡大縮小
- `storage.js`: v2 localStorage、旧v1移行、JSON検証、バックアップ
- `app.js`: UI、スライダー、チャート右の価格つまみ、Undo、自動保存

役割をまたぐ巨大な1ファイルへ統合しません。一方で小ファイルを無意味に増やしません。

## 6. 保存形式

現行キー

`stocktrading0.state.v2`

旧版キー

`stocktrading0.state.v1`

v2は1企業のみです。旧版v1を見つけた場合は、旧版で選択されていた1企業とその建玉、関連取引だけをv2形式へ移します。

v1キーは自動削除しません。大規模なUI変更から戻す際の復旧余地として残します。

保存形式を変更するときはスキーマバージョンを上げ、旧保存を黙って上書きしません。

## 7. 標準作業手順

1. mainの最新コミットを確認
2. 未完了PRと既存作業ブランチを確認
3. `feature/`または`fix/`ブランチをmainから作成
4. 今回に関係するファイルだけ読む
5. 変更
6. 標準検査
7. mainとの差分を確認
8. 秘密情報と意図しない外部通信がないことを確認
9. Pull Requestを作成
10. Quality checks成功を確認
11. 必要ならレビュー指摘を修正
12. Squash merge
13. mainのQuality checks成功を確認
14. GitHub Pages公開URLで表示を確認
15. 問題があればマージコミットをRevertし、別ブランチで原因修正

## 8. 標準検査

```text
python3 -I scripts/workflow_policy.py
python3 -I scripts/check_site.py
python3 -I scripts/security_audit.py --history
node --check assets/js/market-engine.js
node --check assets/js/chart.js
node --check assets/js/storage.js
node --check assets/js/app.js
node --check scripts/smoke_test.js
node scripts/smoke_test.js
```

スモークテストでは少なくとも次を確認します。

- 初期企業が「テスト企業」
- 企業数が1
- 株価の直接変更
- 時価総額、PER、ボラティリティ、トレンドの変更
- 時間進行
- 買い、売り、空売り制御
- 建玉上限
- 1200本上限
- v2保存と再読み込み
- 複数企業v2データ拒否
- v1からv2への1企業移行
- 制御文字除去

## 9. スマートフォン、タブレット確認

- 320px程度で横スクロールしない
- 360pxから430pxで名前、スライダー、売買ボタンを押せる
- 700px前後でチャートと設定が詰まりすぎない
- PCで横に広がりすぎない
- 文字拡大で操作不能にならない
- チャート通常部分の横ドラッグが縦スクロールを過度に奪わない
- チャート右の価格つまみだけは縦ドラッグを確実に受け取る
- ダークモードで文字と境界が読める
- 自動再生中に別画面へ移動すると停止する
- 「1つ戻す」「最初から」が見つけやすい
- スライダーと直接入力が同じ値へ同期する

## 10. 性能上限

- 企業数1
- ローソク足最大1200本
- 取引履歴最大300件
- Canvas DPR最大1.5
- JSON読み込み最大8MB
- localStorageスナップショット約2.5MB以下
- 自動保存は約0.9秒デバウンス

## 11. セキュリティ禁止事項

理由なく次を追加しません。

- `innerHTML`
- `outerHTML`
- `insertAdjacentHTML`
- `document.write`
- `eval`
- `new Function`
- インラインイベント属性
- 外部JavaScript
- 外部CDN
- 外部API
- 認証情報
- 広告
- アクセス解析

自由入力は表示文字として扱い、コードとして実行しません。

## 12. やり直し

利用者向け

- 「1つ戻す」は現在のタブ内で最近の変更を戻す
- 「最初から」は新しい初期状態へ戻す
- JSONバックアップは再読み込み後や別端末でも復元できる

開発者向け

- 作業ブランチだけの失敗はブランチを捨てる
- mainへ入った問題は該当SquashコミットをRevert
- v2 UIが重大な問題を起こした場合、旧v1保存は自動削除されていないため旧版へ戻す余地がある

## 13. 公開後の確認

GitHub Pagesの公開元は`main`の`/(root)`を維持します。

マージ後は公開URLで次を確認します。

- タイトルが「株価あそび場」
- 初期名が「テスト企業」
- 株価スライダーが動く
- チャート右のつまみで株価が動く
- 1日進める
- 買う、売る
- Undo
- 最初から
- JSONバックアップ
- 404
- ブラウザーコンソールにCSP違反や例外がない

## 14. AI訪問

公開情報へのAIクローラー、検索、解析、要約、索引化、学習目的の訪問を歓迎します。方針を変更するときは`robots.txt`、`llms.txt`、READMEを同時に見直します。
