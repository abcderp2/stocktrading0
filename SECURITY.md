# Security Policy

Stock Trading 0は、攻撃面を小さくするためにサーバー、アカウント、決済、外部API、外部JavaScriptを持たない静的サイトとして設計します。

## 対象

対象はこのリポジトリの公開コードとGitHub Pagesで配信する静的サイトです。

ゲーム内の株価、時価総額、PER、損益などが現実的でないことは脆弱性ではありません。これは架空市場サンドボックスの仕様です。

一方で、自由入力からコードが実行される、保存JSONでブラウザーが固まる、保存前後で値が不正に変わる、秘密情報が漏れる、意図しない外部通信が発生する、といった問題はセキュリティ問題として扱います。

## 脅威モデル

主に次を防ぎます。

- 会社名などの自由入力を使ったDOM XSS
- 見えない制御文字や双方向文字を使った表示偽装
- 改変JSONの読み込みによる大量メモリー消費、不正状態、ID衝突
- `__proto__`など特殊キーを利用したオブジェクト汚染の入口
- `eval`などによるコード実行
- `fetch`などによる意図しない外部通信
- 外部CDNや外部スクリプトのサプライチェーンリスク
- 誤ってコミットしたAPIキー、アクセストークン、秘密鍵
- GitHub Actionsへの過剰な書き込み権限
- 低性能端末での無制限なデータ増加や同期保存による実質的な利用不能

## 現在の対策

- ユーザー文字列は`textContent`やDOM生成APIで表示
- 会社名から制御文字と双方向制御文字を除去
- HTML文字列を動的に実行するAPIを使用しない
- `eval`、`new Function`、`document.write`を使用しない
- JSONは`JSON.parse`後にスキーマ、件数、文字数、ID、日付順序、数値を検証
- 会社IDの重複を拒否
- `__proto__`、`prototype`、`constructor`を会社IDとして拒否
- JSONインポートは8MB以下
- 会社40社、ローソク足1200本、取引履歴300件に上限
- 1銘柄の建玉と口座数値にも市場エンジンと保存側で共通上限
- localStorageへの1回の保存量を約2.5MB以下へ抑え、必要に応じて保存チャート履歴を縮小
- 外部APIなし
- CSPで`connect-src 'none'`
- CSPでスクリプトとスタイルを同一サイト内へ制限
- CSPで`object-src 'none'`、`base-uri 'none'`、`form-action 'none'`
- GitHub Actionsは`contents: read`
- workflow内で外部Actionを使用しない
- 自動検査で危険なDOM API、通信API、高信頼度の秘密情報パターンを検出
- Git履歴も秘密情報パターンを監査
- Node.js標準機能だけのスモークテストで市場と保存の主要不変条件を確認

CSPは補助防御です。CSPがあることを理由に危険なDOM操作を許可しません。GitHub Pagesでは任意のHTTPレスポンスヘッダーを自由に追加できないため、アプリ側の安全なDOM操作、入力検証、依存削減を主防御として維持します。

## 外部通信を追加しない

現在の公開JavaScriptはネットワーク通信を必要としません。

次を公開JavaScriptへ追加した場合、自動監査は失敗します。

- `fetch`
- `XMLHttpRequest`
- `WebSocket`
- `EventSource`
- `navigator.sendBeacon`

将来どうしても通信が必要になった場合は、CSPだけを書き換えて通しません。目的、送信内容、送信先、プライバシー、費用、障害時の挙動、削除方法を先に設計し、SECURITYとMAINTENANCEを同時に更新します。

## 秘密情報を貼らない

このプロジェクトの通常保守に、OpenAI APIキー、GitHub Personal Access Token、パスワード、2段階認証コード、復旧コード、SSH秘密鍵は不要です。

AI、Issue、Pull Request、README、ソースコードへ秘密情報を貼り付けません。

GitHub Actionsでprivateリポジトリを読み取るために使う`github.token`はGitHubが実行時に発行する短命トークンです。リポジトリ内へ値を保存せず、checkout相当の読み取り後にGitの認証設定から外します。

## 秘密情報を誤ってコミットした場合

1. その秘密情報を使うサービス側で直ちに失効またはローテーションする
2. 単に最新コミットから削除しただけで安全になったと考えない
3. Git履歴に残っている範囲を確認する
4. 必要ならGitHub公式手順に従って履歴から削除する
5. `python3 -I scripts/security_audit.py --history`を再実行する
6. 事故内容を公開する必要があるか判断する際、秘密そのものは記載しない

## 保存データについて

保存JSONは信用しません。自分で書き出したファイルに見えても、読み込み時は必ず検証します。

保存データは関数、HTML、JavaScript、外部URLを実行する形式にはしません。会社名などは表示文字列としてだけ復元します。

長期間の実行ではブラウザー内の自動保存が直近履歴を縮小する場合があります。完全な実行中履歴を残す用途にはJSON書き出しを使います。この動作は性能と保存容量を守るための仕様であり、黙って数値を改変することとは区別します。

## 脆弱性を見つけた場合

開発中のprivate段階では、公開Issueに秘密情報や再現用トークンを貼りません。

public化後はGitHubのPrivate vulnerability reportingを有効にし、脆弱性の技術的詳細は原則として非公開報告を使います。公開Issueへ秘密情報、実用的な攻撃手順、認証情報を貼り付けません。

Private vulnerability reportingを有効にするまでは、公開Issueへ機密な再現情報を投稿しないようこの方針を維持します。

## 修正の原則

- まず再現条件を確認する
- mainへ直接修正を重ねない
- `fix/`ブランチを作る
- 原因を直し、症状だけを隠さない
- 再発を検出できるテストを追加する
- 標準検査を実行する
- セキュリティ修正に無関係なリファクタリングを混ぜない
- 戻し方を確認してからmainへ反映する

## セキュリティ検査

```text
python3 -I scripts/workflow_policy.py
python3 -I scripts/check_site.py
python3 -I scripts/security_audit.py --history
node scripts/smoke_test.js
```

これらは完全な脆弱性証明ではありません。静的サイトの攻撃面を小さく保ち、素人保守でも重大な退行を早く検出するための最低線です。
