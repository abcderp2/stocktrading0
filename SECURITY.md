# Security Policy

株価あそび場は、攻撃面を小さくするためサーバー、アカウント、決済、外部株価API、外部JavaScriptを持たない静的サイトとして設計します。

## 対象

対象はこのリポジトリの公開コードとGitHub Pagesで配信する静的サイトです。

株価、時価総額、PER、損益などが現実的でないことは脆弱性ではありません。自由に数値を動かせることが仕様です。

一方、自由入力からコードが実行される、改変JSONでブラウザーが固まる、秘密情報が漏れる、意図しない外部通信が発生する、といった問題はセキュリティ問題として扱います。

## 脅威モデル

- 会社名などの自由入力を使ったDOM XSS
- 改変JSONによる不正状態や大量メモリー消費
- `eval`などによるコード実行
- 外部スクリプトのサプライチェーンリスク
- APIキーやアクセストークンの誤コミット
- GitHub Actionsの過剰権限
- 低性能端末での無制限データ増加
- Unicode制御文字を使った表示偽装
- 旧保存形式の移行時に想定外データを引き継ぐこと

## 現在の対策

- 利用者入力はDOM文字列として表示
- HTML文字列を実行するAPIを使用しない
- `eval`、`new Function`、`document.write`を使用しない
- CSPで`connect-src 'none'`
- 公開JavaScriptに意図しない通信APIが入らないことを自動監査
- JSONはサイズ、スキーマ、数値、ローソク足順序を検証
- 現行v2は企業数1を強制
- 旧v1は選択中だった1企業だけをv2へ移行
- 旧v1 localStorageを自動削除しない
- ローソク足、取引履歴、建玉、口座数値に上限
- GitHub Actionsは`contents: read`
- workflow内で外部Actionを使用しない
- Git履歴も高信頼度の秘密情報パターンを監査

CSPは補助防御です。CSPを理由に危険なDOM操作を許可しません。

## 秘密情報を貼らない

通常保守にOpenAI APIキー、GitHub Personal Access Token、パスワード、2段階認証コード、復旧コード、SSH秘密鍵は不要です。

AI、Issue、Pull Request、README、ソースコードへ秘密情報を貼りません。

## 脆弱性を見つけた場合

公開Issueへ秘密情報、アクセストークン、個人情報を貼らないでください。Private vulnerability reportingが利用可能な場合はそれを優先します。

## 秘密情報を誤ってコミットした場合

1. 対象サービスで直ちに失効またはローテーション
2. 最新コミットから削除しただけで安全と考えない
3. Git履歴を確認
4. 必要ならGitHub公式手順で履歴から削除
5. `python3 -I scripts/security_audit.py --history`を再実行
6. 事故説明へ秘密そのものを記載しない

## 修正の原則

- mainへ直接修正を重ねない
- `fix/`ブランチを作る
- 原因を直す
- 標準検査を実行
- 無関係なリファクタリングを混ぜない
- 戻し方を確認してからmainへ反映
